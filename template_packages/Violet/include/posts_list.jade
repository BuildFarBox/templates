.posts
	for post in posts
		bg_class = 'post_bg_one' if loop.index%2 else 'post_bg_two'
		div(class='post_bg '+bg_class)
			.post.post_in_list.common_width
				h2.title
					a(href=post.url)= post.title
				span.date= post.date.format('%Y-%m-%d')
				.post_body= post.content.limit(300).split('</div>', 1)[0]+'</div>'
				.read_more
					a(href=post.url) Read More

include include/paginator